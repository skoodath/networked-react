import styled from 'styled-components';

export const Header = styled.h3`
  font-weight: 500;
  text-transform: uppercase;
  font-size: 2rem;
  letter-spacing: 3px;
  padding: 0;
  margin: 0 0 2rem 0;
  position: relative;

`;